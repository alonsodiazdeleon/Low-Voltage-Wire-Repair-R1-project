# Notebooks

Use for:

- Profiling `connector_number` / `circuit` / `reapair_location` value distributions (unmapped %).
- Validating join keys on `dim_sbom_parts` and `dim_fct_catia_ebom_parts` for R1%.
- Jobs that refresh Delta mapping tables from CSV in `/Volumes` or repo.

Do not commit secrets or large extracts; use Databricks secrets and scoped paths.
